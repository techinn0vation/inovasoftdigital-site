// // Todos os componentes que sairem de "componentes", deveram passar por aqui antes de serem adicionados as paginas.
import { Header } from "../Header";
import { Button } from "../Button";

export { Header, Button };
